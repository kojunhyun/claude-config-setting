# Per-machine local overrides
settings.local.json
paths.local.env
.claude.json

# Auth / secrets
.credentials.json
*.token
*.key

# Caches / runtime
cache/
backups/
file-history/
session-env/
sessions/
shell-snapshots/
projects/
debug/
paste-cache/
image-cache/
channels/
tasks/
plugins/
ide/

# Hook state
.caveman-active
.last-cleanup
.last-update-result.json
policy-limits.json
mcp-needs-auth-cache.json
remote-settings.json
history.jsonl

# OS junk
.DS_Store
Thumbs.db

# Claude Code runtime telemetry junk (machine-local, not config)
telemetry/

# security-guidance plugin runtime state (machine-local, 1.5k+ session files)
security/
